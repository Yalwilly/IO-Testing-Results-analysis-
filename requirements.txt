# IO Testing Results Analysis - Python Standard Library Only
# No external packages required.
# All dependencies use Python's built-in modules:
#   csv, zipfile, xml.etree.ElementTree, statistics, pathlib, math, logging
#
# Optional (only if you want to run the unit tests):
# pytest>=7.0.0

numpy>=1.23.0
matplotlib>=3.6.0
openpyxl>=3.0.0
python-pptx>=0.6.21
jinja2>=3.1.0
scipy>=1.9.0
